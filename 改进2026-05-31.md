# 改进说明 2026-05-31

## 1. 本次改进目标

本次改进围绕两个核心问题展开：

1. 让项目能够直接识别当前 `.env` 中配置的 DeepSeek API。
2. 优化流程输出格式，使结果更适合人工查看、程序消费和后续扩展。

---

## 2. 已完成的代码改进

### 2.1 LLM 配置接入增强

已增强 `src/influence_strategy/llm_client.py`，现在支持以下环境变量格式：

- `LLM_API_KEY`
- `LLM_BASE_URL`
- `LLM_MODEL`
- `DEEPSEEK_API_KEY`
- `DEEPSEEK_BASE_URL`
- `DEEPSEEK_MODEL`
- `OPENAI_API_KEY`
- `OPENAI_BASE_URL`
- `OPENAI_MODEL`

同时新增了：

- `provider` 自动识别
- `describe()` 方法，便于把模型来源写入输出元信息
- `.env.example` 模板文件，便于后续迁移和复用

这意味着你当前使用的 `.env` 形式已经可以被项目识别，不需要再改成 OpenAI 命名才能工作。

---

### 2.2 `run_eval.py` 输出结构标准化

此前 `run_eval.py` 的输出是“顶层动态字段 + 中文键名”的形式，例如：

- `事件名称`
- `选取数字人id组`
- `id81584`

这种结构不利于：

- 程序解析
- 前端展示
- 后续落数据库
- 批量评测对比

现在已经统一为稳定的标准结构，核心字段如下：

```json
{
  "meta": {},
  "event": {},
  "summary": {},
  "stage_plans": [],
  "five_dimensions": {},
  "selected_digital_humans": [],
  "fallback_digital_humans": [],
  "risk_control": {},
  "explainability": []
}
```

其中：

- `meta`：记录 schema 版本、生成时间、LLM 使用状态、provider、model、输出路径
- `event`：记录热点事件原始信息
- `summary`：记录主平台、风险等级、主选数量、备选数量、角色分布等摘要
- `stage_plans`：记录阶段计划
- `five_dimensions`：统一整理为五个维度的策略结果
- `selected_digital_humans`：主选数字人数组
- `fallback_digital_humans`：备选数字人数组
- `risk_control`：风险控制方案
- `explainability`：解释性文本

---

### 2.3 数字人输出结构格式化

主选数字人现在不再使用 `idxxxx` 这种动态键，而是统一放进数组：

```json
{
  "selected_digital_humans": [
    {
      "selection_rank": 1,
      "user_id": "81584",
      "selected_role": "core_publish_node",
      "stage_text": "启动期（T0 - T+2h）",
      "frequency_text": "2/day",
      "platform": "weibo_simulated",
      "final_score": 0.78,
      "content_output": {
        "post_content": "...",
        "audience_profile": "...",
        "audience_interaction_strategy": "...",
        "cross_digital_human_ids": [],
        "cross_digital_human_strategy": "..."
      }
    }
  ]
}
```

改进点：

- 更适合排序、分页、检索
- 更适合前端表格展示
- 更适合后续 API 返回
- 更适合做批量评测分析

---

### 2.4 增加 Markdown 报告输出

为了让结果更适合直接阅读，本次新增了 Markdown 报告输出：

- `main.py` 现在会同时输出：
  - `strategy_<event_id>.json`
  - `strategy_<event_id>.md`
- `run_eval.py` 现在会同时输出：
  - `<event_id>_strategy_output.json`
  - `<event_id>_strategy_output.md`
  - 批量评测汇总 `eval/output/output.md`

Markdown 报告包含：

- 事件概览
- 生成元信息
- 五个维度
- 主选数字人表
- 备选数字人表
- 风险控制
- 可解释性说明
- 主选数字人执行细节

这使得输出同时具备：

- JSON：结构化、可编程
- Markdown：可读、可汇报

---

### 2.5 增加 LLM 使用元信息

现在 `run_eval.py` 输出中会明确记录本次是否真的用了大模型，而不是只看有没有开启参数。

新增字段示例：

```json
{
  "meta": {
    "generator_mode": "llm_enhanced",
    "llm": {
      "requested": true,
      "configured": true,
      "attempted": true,
      "used": true,
      "provider": "deepseek",
      "model": "deepseek-v4-flash",
      "base_url": "https://api.deepseek.com",
      "fallback_reason": null
    }
  }
}
```

可能的模式包括：

- `rule_only`
- `llm_enhanced`
- `llm_fallback`

这样后续做实验、评测、对比时，可以明确知道：

- 这次有没有请求 LLM
- 是否真的成功使用了 LLM 结果
- 是否是规则模板兜底

---

### 2.6 测试补强

本次新增或更新了以下测试：

- `tests/test_eval_hot_events.py`
- `tests/test_llm_client.py`

测试覆盖了：

- `LLM_*` 形式的 `.env` 是否能正确识别为 DeepSeek 配置
- 热点事件评测输出是否生成新的标准 JSON 结构
- 是否会同步生成 Markdown 报告
- LLM 返回内容是否能正确覆盖规则模板
- 批量评测是否会生成汇总 Markdown

当前测试结果：

```text
17 passed
```

---

## 3. 本次新增文件

- `.env.example`
- `src/influence_strategy/reporting.py`
- `tests/test_llm_client.py`
- `改进2026-05-31.md`

同时重构了：

- `main.py`
- `run_eval.py`
- `src/influence_strategy/eval_hot_events.py`
- `src/influence_strategy/llm_client.py`
- `tests/test_eval_hot_events.py`

---

## 4. 现在如何使用

### 4.1 单事件策略生成

```powershell
python main.py --event-text "希望围绕亲子阅读与英语启蒙做一次传播活动，提升讨论度并控制风险"
```

输出：

- `outputs/strategy/strategy_<event_id>.json`
- `outputs/strategy/strategy_<event_id>.md`

### 4.2 热点事件评测

启用 LLM：

```powershell
python run_eval.py --event-id hot_event_001
```

关闭 LLM，仅使用规则模板：

```powershell
python run_eval.py --event-id hot_event_001 --disable-llm
```

输出：

- `eval/output/<event_id>_strategy_output.json`
- `eval/output/<event_id>_strategy_output.md`
- `eval/output/output.md`

---

## 5. 当前仍需注意的事项

### 5.1 数据集仍然是运行前提

项目核心流程仍然依赖：

- `data/raw/abc_reading_profile.graph.anon`
- `data/raw/abc_reading_interaction.graph.anon`
- `data/raw/abc_reading_product_info.json`
- `data/derived/abc_reading_profile_with_neighbors.graph.anon`

如果当前工作区没有这些数据文件，`main.py` 和 `run_eval.py` 仍然无法直接跑通。

也就是说，本次改进解决的是：

- LLM 接入
- 输出结构
- 报告格式

但不替代数据本身。

### 5.2 LLM 是增强层，不是替代层

当前实现仍然遵循原来的设计原则：

- 节点筛选与评分依然由规则和图特征完成
- LLM 只负责增强部分文案内容
- 如果没有配置 API 或调用失败，会自动退回规则模板

这保证了：

- 项目可解释
- 项目可复现
- 即使没有 API 也能工作

---

## 6. 建议的下一步

建议后续继续推进这几个方向：

1. 统一 `main.py` 的 JSON schema，也加入 `meta` 和更稳定的摘要字段
2. 给 `README.md` 做一次同步更新，补上新的输出结构说明
3. 如果后续要做前端展示，可以直接以 `selected_digital_humans` 和 `stage_plans` 为基础
4. 如果后续要接更多模型，只需要继续扩展 `llm_client.py` 的环境变量识别规则
5. 为缺失数据文件的情况增加更明确的启动前检查和报错提示

---

## 7. 总结

这次改进后，项目已经从“能生成策略”进一步走向：

- 能识别 DeepSeek `.env`
- 能区分规则输出和 LLM 增强输出
- 能生成标准 JSON
- 能生成 Markdown 报告
- 能做更稳定的批量评测

对后续扩展 API、前端界面、实验对比和结果归档都更友好了。
