# 2026-06-02 热点评测流水线改造说明

## 1. 本轮改动目标

本轮修改主要围绕 `hot_event` 评测链路展开，目标有三个：

1. 降低 `abc_reading` 数据集原有“商品推荐/高影响力节点优先”偏置对事件分发任务的干扰。
2. 将输出格式改回约定的 `.json` 结构，只保留最终策略 JSON，不再生成额外 Markdown 结果文件。
3. 为整条流水线增加可观测性，能够直接查看 `event_parser -> feature_builder -> scorer -> selector -> strategy_generator` 每一步的输入输出。


## 2. 主要修改内容

### 2.1 输出层改造

本次将热点评测输出统一收敛到旧版约定结构，即：

- `事件名称`
- `选取数字人id组`
- 对每个选中数字人，输出：
  - `时间阶段`
  - `发帖频率`
  - `发帖平台`
  - `发帖内容`
  - `目标受众`
  - `与其他数字人互动策略`

对应修改位置：

- `src/influence_strategy/eval_hot_events.py`
- `run_eval.py`

同时移除了批量 Markdown 结果输出逻辑，当前 `run_eval.py` 只生成：

- `eval/output/<event_id>_strategy_output.json`


### 2.2 流水线追踪能力

新增了中间结果追踪输出目录：

- `tests/pipeline_step_outputs/<event_id>/`

每次运行 `run_eval.py` 时，会自动落盘以下文件：

- `00_hot_event_input.json`
- `01_pipeline_payload.json`
- `02_event_parser_output.json`
- `03_feature_builder_output.json`
- `04_scorer_output.json`
- `05_selector_output.json`
- `06_strategy_generator_output.json`
- `07_final_output.json`

这样可以直接检查：

- `event_parser` 把事件解析成了哪些关键词、语义标签、角色偏好
- `feature_builder` 给候选节点打了哪些主题相关和语义特征
- `scorer` 最终是如何过滤和排序节点的
- `selector` 到底选了哪些节点、分配了什么角色

对应修改位置：

- `src/influence_strategy/pipeline.py`
- `src/influence_strategy/eval_hot_events.py`
- `run_eval.py`


### 2.3 `event_parser` 改造

为了让事件解析不再过度依赖原始数据集的阅读/推荐语境，本轮对 `event_parser` 做了以下增强：

- 保留规则解析基础能力。
- 增加更通用的关键词抽取逻辑，不再只依赖原来的阅读类词表。
- 从 `event_title`、`event_description`、`target_goal` 中提取更一般化的事件词。
- 为热点事件拼接更明确的任务描述，强调“这是影响力事件分发，不是商品推荐”。
- 保留 LLM 解析入口，作为规则解析的增强层。

对应修改位置：

- `src/influence_strategy/event_parser.py`
- `src/influence_strategy/prompts.py`


### 2.4 `feature_builder` 改造

`feature_builder` 是本轮最关键的改造点之一，主要包括：

- 加入事件标题参与关键词匹配。
- 去掉 `general_public` 这类泛标签对匹配的正向影响，避免“所有人都能匹配”。
- 收紧模糊匹配规则，避免短 token、乱码 token 或泛化词误命中。
- 增加候选池多样化逻辑，避免 top candidates 被高粉泛账号垄断。
- 增强 LLM 返回结果解析的容错能力，不再只接受单一 `nodes` 结构。
- 支持从多种 key 风格中提取 LLM 语义打分结果。

本次还修复了一个非常关键的问题：

- 之前真实运行时，`feature_builder` 的 LLM 增强并没有稳定生效，导致大量节点的 `llm_feature_score` 一直是 `0.0`。
- 现在已经补上更宽松的响应解析和候选映射逻辑，真实 LLM 路径可以正常写回语义相关分数。

对应修改位置：

- `src/influence_strategy/feature_builder.py`
- `src/influence_strategy/prompts.py`


### 2.5 `scorer` 改造

为了压制“高影响力但主题不相关”的节点，本轮对打分阶段做了更严格的门控：

- 强化 `semantic_gate`，要求节点必须具备足够主题证据或 LLM 语义证据。
- 新增对 `generic_high_influence_mismatch` 的风险惩罚。
- 下调单纯影响力分数的权重。
- 提高主题相关、稳定性、语义匹配在总分中的权重。
- 对高风险事件进一步提高主题和风险控制权重。

换句话说，现在节点不能再仅靠：

- 粉丝多
- 邻居多
- 互动多

就进入主选集合；必须证明自己与当前事件主题确实相关。

对应修改位置：

- `src/influence_strategy/scorer.py`


### 2.6 `selector` 与策略生成保持联动

选择阶段保留了原有的角色分配和 LLM rerank 机制，但现在输入候选已经被更严格地清洗过，因此效果更稳定。

主要表现为：

- `selector` 不再只是在一堆泛高分账号里重排。
- LLM rerank 现在是在“更合理的候选池”中做角色分配。
- `strategy_generator` 继续基于最终选中的节点生成阶段、频率、内容与协同方案。

对应修改位置：

- `src/influence_strategy/selector.py`
- `src/influence_strategy/strategy_generator.py`


## 3. 当前流水线工作过程

现在执行 `run_eval.py` 时，整体流程如下：

### Step 1. 读取热点事件

入口脚本：

- `run_eval.py`

负责：

- 读取 `eval/hot_event_opinion_variants.json`
- 支持 `--event-id` 选取单个事件
- 支持 `--disable-llm` 关闭 LLM 增强


### Step 2. 将热点事件转换为内部事件输入

函数：

- `hot_event_to_pipeline_payload()`

作用：

- 将原始 `hot_event` 转换为内部统一事件结构
- 注入：
  - `event_id`
  - `event_title`
  - `event_description`
  - `target_goal`
  - `target_audience`
  - `constraints`


### Step 3. `event_parser`

模块：

- `src/influence_strategy/event_parser.py`

作用：

- 把事件文本解析成结构化事件对象
- 输出：
  - `event_type`
  - `target_goal`
  - `target_audience`
  - `extracted_keywords`
  - `semantic_tags`
  - `target_roles`
  - `dispatch_preferences`

如果启用 LLM：

- 会在规则解析基础上做一次结构化语义增强


### Step 4. `feature_builder`

模块：

- `src/influence_strategy/feature_builder.py`

作用：

- 从数据集中读取 profile、interaction、graph attributes
- 为每个候选节点构建：
  - 影响力特征
  - 扩散能力特征
  - 稳定性特征
  - 关键词命中特征
  - 语义标签

如果启用 LLM：

- 对候选池做语义增强
- 写回：
  - `event_semantic_relevance_score`
  - `audience_fit_score`
  - `role_fit_score`
  - `narrative_fit_score`
  - `risk_conflict_score`
  - `llm_feature_score`


### Step 5. `scorer`

模块：

- `src/influence_strategy/scorer.py`

作用：

- 结合规则特征与 LLM 特征计算总分
- 判断节点是否：
  - `eligible`
  - `manual_review_required`
  - 属于哪一类 `risk_level`


### Step 6. `selector`

模块：

- `src/influence_strategy/selector.py`

作用：

- 从 `eligible` 候选中选出主选节点与 fallback 节点
- 为选中节点分配：
  - `core_publish_node`
  - `interaction_response_node`
  - `amplification_node`
  - `support_node`

如果启用 LLM：

- 对 shortlist 做 rerank 和角色建议


### Step 7. `strategy_generator`

模块：

- `src/influence_strategy/strategy_generator.py`

作用：

- 将选中节点转换为可执行分发策略
- 生成：
  - 时间阶段
  - 发布频率
  - 平台
  - 建议动作
  - 协同关系


### Step 8. 最终输出与中间追踪

模块：

- `src/influence_strategy/eval_hot_events.py`

作用：

- 生成最终 `.json`
- 将整条流水线的每个阶段写入 `tests/pipeline_step_outputs/<event_id>/`


## 4. 本轮改动后的实际效果

本轮修改后，已经能看到几个明显变化：

1. 最终输出格式已经回到约定结构。
2. 每一步中间结果都可以单独检查，不再是黑盒。
3. 真实 LLM 路径下，`feature_builder` 的语义增强已经能生效。
4. 之前频繁出现的某些明显不相关高影响力节点，已经不再稳定占据主选集合。

同时也要说明，目前效果还没有完全达到“强主题对齐”的理想状态，原因主要有：

- 数据集本身并不是围绕国际热点事件构建的
- 账号画像仍然偏生活化、阅读化、泛社交化
- 即使经过门控，仍可能出现“局部相关但不够理想”的节点

因此，当前版本更适合作为：

- 一版可运行、可解释、可继续迭代的基线系统


## 5. 验证情况

本轮已经完成以下验证：

- 单元测试全部通过
  - `conda run -n abc-reading-strategy python -m pytest`
  - 结果：`24 passed`
- 实际运行 `hot_event_001`
  - 规则模式验证
  - LLM 增强模式验证
- 中间追踪输出已成功写入：
  - `tests/pipeline_step_outputs/hot_event_001/`


## 6. 后续可继续改进的方向

### 6.1 继续压制伪相关节点

目前仍建议继续加强：

- 对生活方式类、旅游类、鸡汤类账号的负向识别
- 对国际事件、航运、金融、军事、政策类事件的“主题白名单/黑名单”
- 对节点描述与兴趣词的更强语义筛查


### 6.2 引入更稳定的语义检索层

当前仍以规则 + LLM 打分为主，后续可以考虑：

- 使用 embedding 建立事件与节点画像之间的语义相似度检索
- 在 `feature_builder` 前增加一个向量召回层
- 先召回更相关的小候选池，再交给 LLM 精排


### 6.3 增加事件类型专用模板

当前热点事件仍然走较通用模板，后续可按事件类型细分：

- 军事 / 地缘冲突
- 金融 / 利率 / 市场波动
- 网络安全 / 数据泄露
- 气候 / 灾害 / 应急
- 公共政策 / 监管

不同模板可分别约束：

- 目标受众
- 优先角色
- 风险控制
- 负向表达约束


### 6.4 加入节点负样本规则

可以考虑在 `feature_builder` 或 `scorer` 中显式增加：

- `topic_negative_match_score`
- `lifestyle_bias_penalty`
- `over_generic_profile_penalty`

使不相关但高活跃账号在前期就被更早压低。


### 6.5 增加缓存与批处理

如果后续要大规模跑 `hot_event`，建议增加：

- LLM 响应缓存
- 中间结果缓存
- 候选池分批语义增强
- 按事件并行执行

这样可以显著降低：

- API 开销
- 重复调用时间
- 调试成本


### 6.6 改善可视化与人工分析工具

目前已经有 JSON 追踪结果，下一步可以补：

- 一个 trace summarizer
- 一个简单的可视化对比脚本
- 主选节点与 fallback 节点对比表
- “为什么这个节点被选中/没选中”的自动摘要


## 7. 建议的下一步工作

如果继续推进，建议优先做下面两件事：

1. 基于 `tests/pipeline_step_outputs/hot_event_001/` 的真实中间结果，逐个分析仍然偏泛化的节点，继续打磨候选门控。
2. 为不同类型热点事件设计更专用的 LLM 提示词和负约束模板，减少“数据集原始主题”对最终选人的干扰。


## 8. 文档结论

截至 2026-06-02，本项目已经从“只能输出最终策略、难以解释过程”的状态，改造成：

- 支持规则 + LLM 的多阶段分发流水线
- 支持 JSON 结构化输出
- 支持逐步追踪和调试
- 支持对高影响力但主题不相关节点进行更严格抑制

这为后续继续提升“事件主题对齐能力”和“选人可信度”打下了一个比较扎实的基础。
