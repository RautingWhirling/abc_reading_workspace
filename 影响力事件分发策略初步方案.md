# 影响力事件分发策略初步方案

## 1. 文档定位

这是一份面向当前 `abc_reading` 数据集的初步实施方案，目标不是构建完整工程，也不是直接落地真实平台发布，而是先形成一条可行、清晰、便于后续扩展的流水线：

```text
事件输入 -> 候选人物识别 -> 影响力与风险评估 -> 分发策略生成
```

本方案刻意避免将任务理解为“商品推荐”或“商品营销”。  
当前更合适的表述是：

- 以数据集中的匿名用户作为“候选传播节点”
- 以互动网络作为“传播关系图”
- 以输入事件作为“待分发的影响力事件”
- 以输出结果作为“结构化事件分发策略”

## 2. 当前阶段目标

当前阶段只追求一个轻量、可解释、可扩展的 baseline：

- 接收事件描述输入
- 读取 `abc_reading` 的画像与互动数据
- 从互动图中识别适合参与分发的人物
- 生成结构化分发策略
- 为后续接入大模型 API 预留接口位置

当前阶段不做：

- 不连接真实社交平台
- 不做复杂前后端系统
- 不追求高并发和性能优化
- 不做多模态输入
- 不把数据集强行解释成商品推荐场景

## 3. 可行性判断

从数据角度看，这个任务是可行的。

`abc_reading` 数据集中已经具备三类关键基础：

- 用户画像：粉丝数、关注数、兴趣标签、简介文本
- 互动关系：评论和转发形成的有向传播边
- 邻居关系增强结果：可以直接用于判断局部传播结构

因此，虽然数据中没有真实“数字人”实体，也没有跨平台数据，但足以支持一个“基于历史互动网络的事件分发策略生成原型”。

需要明确的边界：

- 这里选出的不是现实中的数字人，而是“候选传播角色原型”
- 输出的不是平台执行指令，而是“结构化策略建议”
- 跨平台能力只能作为规则模拟，不能作为真实观测能力

## 4. 数据集使用原则

### 4.1 数据使用范围

当前只使用以下文件：

- `data/raw/abc_reading_profile.graph.anon`
- `data/raw/abc_reading_interaction.graph.anon`
- `data/derived/abc_reading_profile_with_neighbors.graph.anon`

可选参考文件：

- `data/raw/abc_reading_product_info.json`

### 4.2 数据语义重解释

为了脱离“商品推荐”主题，建议对字段语义做如下调整：

| 原始对象 | 在本方案中的解释 |
| --- | --- |
| profile 用户 | 候选传播节点 |
| interaction 互动记录 | 传播边 |
| followers/friends | 节点规模特征 |
| interests | 话题/圈层偏好 |
| user_description | 角色画像文本 |
| product_info | 可选的背景上下文，不作为核心依赖 |

### 4.3 不建议直接使用的解释

以下解释在当前阶段应避免：

- 不把用户直接当作“带货账号”
- 不把任务定义成“商品推广”
- 不把 `user_friends` 解释成真实关注关系图
- 不把跨平台分发写成已有数据支撑的真实能力

## 5. 简化后的总体流程

建议将原来的复杂流程压缩为 5 个核心步骤。

### 步骤 1：事件输入结构化

输入可能是一段自然语言，例如：

> 希望围绕亲子阅读与英语启蒙做一次高影响力传播，目标是提升讨论度并控制刷屏风险。

系统先把它整理成统一结构，例如：

```json
{
  "event_id": "event_001",
  "event_title": "亲子阅读传播活动",
  "event_description": "围绕亲子阅读与英语启蒙进行内容传播",
  "target_goal": "提升讨论度",
  "target_audience": ["亲子", "阅读", "英语启蒙"],
  "constraints": {
    "risk_level": "medium",
    "max_selected_nodes": 10,
    "max_frequency_per_day": 3
  }
}
```

这一层的目标只是把输入整理干净，不做复杂推理。

### 步骤 2：候选传播节点构建

基于画像数据和互动数据，为每个用户构建基础画像：

- 节点规模：粉丝数、关注数
- 互动强度：被评论数、被转发数、主动互动数
- 图结构位置：邻居数、双向邻居数、局部连接情况
- 文本线索：兴趣标签、简介关键词

这一步的输出不是最终策略，而是一批可参与分发的候选节点集合。

### 步骤 3：影响力与匹配度评估

对每个候选节点计算一个简化评分，建议只保留 4 类核心信号：

- 影响力：粉丝规模、被互动强度
- 扩散力：转发相关活跃度、邻居覆盖度
- 匹配度：兴趣标签与事件主题的相关度
- 稳定性：是否存在异常高频、自环过多、画像缺失等情况

可以先使用简单可解释规则，例如：

```text
final_score =
  0.35 * influence_score
+ 0.25 * diffusion_score
+ 0.25 * topic_match_score
+ 0.15 * stability_score
```

第一版不需要追求复杂模型，重点是评分逻辑可解释。

### 步骤 4：候选角色分配

根据评分特征，把高分节点分成少量角色即可，不必过细。

建议保留 3 类角色：

- 核心发布节点：影响力高、主题匹配高
- 互动响应节点：评论互动强、适合答疑与带讨论
- 扩散放大节点：转发倾向强、适合二次扩散

这样既能覆盖主要场景，也不会把初版方案做得过重。

### 步骤 5：结构化策略生成

最终输出的不是一句自然语言建议，而是一份结构化策略。

建议至少包含以下 5 个维度：

- 分发对象：选中了哪些节点，各自扮演什么角色
- 时间安排：首发窗口、扩散窗口、回收窗口
- 频率安排：每类节点的建议触发次数
- 平台安排：当前以微博语境为主，其他平台只做模拟说明
- 内容安排：每类节点建议使用的表达方式

## 6. 推荐输出结构

建议输出一个轻量 JSON 结构，类似：

```json
{
  "event_id": "event_001",
  "event_type": "reading_promotion",
  "selected_nodes": [
    {
      "user_id": "201455",
      "role": "核心发布节点",
      "score": 0.87,
      "reason": ["亲子教育相关", "互动覆盖高", "双向邻居较多"]
    }
  ],
  "strategy": {
    "target_object": ["亲子阅读圈层", "英语启蒙兴趣群体"],
    "time_plan": {
      "stage_1": "T0 - T+2h",
      "stage_2": "T+2h - T+8h",
      "stage_3": "T+8h - T+24h"
    },
    "frequency_plan": {
      "core_nodes": "1-2次",
      "interaction_nodes": "2-3次",
      "amplification_nodes": "1-2次"
    },
    "platform_plan": {
      "primary_platform": "weibo_simulated",
      "secondary_platforms": ["optional_simulated"]
    },
    "content_plan": {
      "core_nodes": "观点型首发内容",
      "interaction_nodes": "问答式互动内容",
      "amplification_nodes": "摘要式扩散内容"
    }
  },
  "risk_control": {
    "risk_level": "medium",
    "review_required": true,
    "notes": ["避免高频重复表达", "避免集中使用高风险节点"]
  }
}
```

这个结构已经足够支持后续 API 化，也便于大模型结构化输出。

## 7. 建议保留的最小模块

为了避免方案过重，当前阶段只建议保留 6 个逻辑模块。

### 7.1 data_loader

负责读取：

- `profile`
- `interaction`
- `neighbor_enriched_profile`

输出统一的 Python 数据结构。

### 7.2 event_parser

负责把事件描述整理成统一输入结构。

这一层先用规则即可，后续再替换成大模型解析。

### 7.3 feature_builder

负责为每个节点构建基础特征：

- 粉丝规模
- 被互动次数
- 主动互动次数
- 邻居数
- 双向邻居数
- 兴趣匹配度

### 7.4 scorer

负责计算：

- 影响力分
- 主题匹配分
- 稳定性分
- 综合得分

### 7.5 selector

负责选人和分配角色。

### 7.6 strategy_generator

负责生成结构化策略输出。

## 8. 大模型 API 的接入位置

当前阶段不要求接入大模型，但方案设计应为后续留好位置。

建议只在以下 3 个环节考虑接入大模型：

### 8.1 事件解析

将自然语言事件描述转为结构化字段：

- 事件主题
- 目标
- 风险偏好
- 受众圈层

### 8.2 内容风格生成

在节点和角色选定之后，由大模型生成：

- 核心发布内容风格
- 互动回复风格
- 扩散节点摘要风格

### 8.3 策略解释

大模型可用于生成更自然的 explainability 文本，例如：

- 为什么选这个节点
- 为什么安排这个时间窗口
- 为什么使用这种频率

不建议在第一版就把“节点筛选主逻辑”完全交给大模型。  
第一版仍应以规则和显式特征为主，大模型只做增强层。

## 9. 实施复杂度建议

为了保证这是一份真正可执行的初步方案，建议复杂度控制如下：

### 必做

- 事件结构化输入
- 节点基础特征构建
- 简单评分
- 角色划分
- 结构化策略输出

### 可选

- 简单负面词风险提示
- 简单异常高频检测
- explainability 文本生成

### 暂缓

- 跨平台真实联动
- 复杂图神经网络
- 多智能体协同
- 自动化在线学习
- 实时流式推断

## 10. 若后续进入实现阶段的环境建议

当前文档不要求立刻建项目，但如果后续进入实现阶段，建议环境尽量简单。

### 10.1 Python 版本

- Python `3.11`

### 10.2 建议依赖

- `pydantic`
- `networkx`
- `pandas` 或 `polars`
- `rapidfuzz`
- `jieba`
- `pytest`

如果后续接入 API，再补：

- `fastapi`
- `uvicorn`
- `httpx`
- `python-dotenv`

### 10.3 目录建议

当前工作目录已经足够：

```text
abc_reading_workspace/
  data/
    raw/
    derived/
  scripts/
  影响力事件分发策略初步方案.md
```

## 11. 最终建议

如果后续要真正开始实现，建议顺序是：

1. 先把事件输入结构固定下来
2. 再把节点特征和评分做成离线可复用结果
3. 最后再做策略拼装
4. 大模型 API 放在事件解析和内容生成层，而不是一开始就替代全部逻辑

这样做的好处是：

- 方案轻
- 边界清楚
- 数据使用合理
- 可解释性强
- 便于后续逐步接入大模型能力

---

一句话概括当前推荐路线：

> 基于 `abc_reading` 互动网络，把匿名用户视作候选传播节点，以规则和图特征为基础，先实现一个可解释的“影响力事件分发策略生成”流水线，再逐步接入大模型能力。
