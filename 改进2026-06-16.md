# 改进2026-06-16

## 本轮改动目标

本轮工作的目标是两件事：

1. 让项目默认使用更轻量的 `compact` 数据集，减少冗余数据对流水线的拖累。
2. 让最终输出从“自然语言描述型策略”升级为“动作化、可执行的结构化策略”。

---

## 已完成改动

### 1. 引入 compact 数据集并接入主流程

- 新增脚本：[scripts/build_compact_dataset.py](./scripts/build_compact_dataset.py)
- 生成文件：
  - `data/derived/abc_reading_profile_compact.graph.anon`
  - `data/derived/abc_reading_profile_compact_summary.json`

本次 compact 数据集保留了当前主流程真正使用的核心字段：

- `user_id`
- `user_name`
- `user_followers`
- `user_friends`
- `user_interests`
- `user_description`
- `graph_attributes`

其中保留的 `graph_attributes` 主要包括：

- `neighbor_count`
- `mutual_neighbor_count`
- `self_interaction_count`
- `received_interaction_count`
- `received_comment_count`
- `received_repost_count`
- `made_interaction_count`
- `made_comment_count`
- `made_repost_count`
- `isolated`

被删除的大块冗余主要包括：

- 完整 `neighbors` 列表
- `engaged_by_neighbor_count`
- `engaged_to_neighbor_count`
- `self_comment_count`
- `self_repost_count`

压缩结果：

- 原增强文件大小约 `124 MB`
- compact 文件大小约 `42 MB`
- 压缩比例约 `66.16%`
- 节点数量保持不变：`70258`

### 2. DataLoader 默认优先读取 compact 数据集

修改文件：[src/influence_strategy/data_loader.py](./src/influence_strategy/data_loader.py)

当前读取优先级：

1. `data/derived/abc_reading_profile_compact.graph.anon`
2. `data/raw/abc_reading_profile.graph.anon`
3. `data/derived/abc_reading_profile_with_neighbors.graph.anon`

这意味着当前主流程在不改命令的情况下，会自动优先使用 compact 数据集。

### 3. 最终输出改为动作化 schema

修改文件：[src/influence_strategy/eval_hot_events.py](./src/influence_strategy/eval_hot_events.py)

新增输出版本：

- `action_schema_v2`

顶层新增字段：

- `输出格式版本`
- `策略概览`

每个数字人节点不再只输出一段“策略描述”，而是拆为四组动作：

- `内容发布动作`
- `受众互动动作`
- `数字人协同动作`
- `风险控制动作`

每条动作统一包含：

- `动作类型`
- `动作名称`
- `执行窗口`
- `触发条件`
- `目标对象`
- `执行参数`
- `目标结果`

### 4. 互动策略从文本改为动作

本次重点改动就是把“互动策略”从泛文本转换为执行动作，例如：

- `monitor_comments`
- `like_comment`
- `reply_comment`
- `comment_on_post`
- `like_post`
- `reply_agent_comment`
- `monitor_sentiment`
- `pause_dispatch_if_needed`

这样做的好处是：

- 后续可以直接接动作执行器
- 前端更容易渲染成按钮 / 时间线 / 执行面板
- 可解释性更强，可以清楚看到每个节点该做什么动作

### 5. 协同策略也改为动作化

过去“与其他数字人互动策略”更多是解释性文本，例如：

- “与某节点联动”
- “围绕某主帖补充”

现在改成明确动作，例如：

- 对哪个数字人的主帖评论
- 对哪个数字人的帖子点赞
- 在什么时间窗口执行
- 执行目的是什么

### 6. 输出格式说明同步更新

修改文件：[eval/output/output.md](./eval/output/output.md)

该文件现在与实际生成 JSON 保持一致，作为 `action_schema_v2` 的说明文档使用。

### 7. 新流程图已重绘

文件：[流程图.svg](./流程图.svg)

这次流程图已经同步反映：

- compact 数据集接入
- DataLoader 默认优先级
- `strategy_generator` 仍是规则编排
- 最终输出改为动作化 `action_schema_v2`

---

## 当前流水线工作方式

当前核心流程仍然是：

```text
event_input
  -> data_loader
  -> event_parser
  -> feature_builder
  -> scorer
  -> selector
  -> strategy_generator
  -> eval_hot_events.build_eval_output
  -> action_schema_v2 JSON
```

其中：

- `event_parser`：负责把事件转成结构化传播任务
- `feature_builder`：负责节点召回和特征构建
- `scorer`：负责风险和综合打分
- `selector`：负责角色覆盖式选人
- `strategy_generator`：负责把选人结果编排成传播计划
- `eval_hot_events`：负责把最终结果翻译成适合评测和执行的动作化 JSON

---

## 当前版本的定位

当前版本不是“真实自动执行系统”，而是“接近可执行的动作计划生成器”。

也就是说：

- 现在已经能输出“谁在什么时候做什么动作”
- 但还没有真正接入自动点赞 / 自动评论 / 自动转发的执行层

因此当前输出更适合作为：

- 执行编排草案
- 前端展示数据
- 后续自动化模块的输入

---

## 当前仍存在的限制

### 1. 数据集主题仍然存在天然偏差

虽然已经切换到 compact 数据集，但 `abc_reading` 本身并不是为“国际热点事件分发”设计的，因此节点语义基础仍然偏弱。

### 2. `strategy_generator` 仍是规则模板

当前 `strategy_generator` 主要负责：

- 时间规划
- 频率规划
- 平台规划
- 内容风格规划
- 节点编排

它还没有直接使用 LLM 来动态改写动作策略，因此策略编排层仍然偏“稳定、规则化”，不是“高度自适应生成”。

### 3. 部分样例输出尚未全部刷新

本轮已完成新 schema 的代码改造与样例格式切换，但仓库中的旧输出样例可能仍然混有旧格式结果。后续建议逐步刷新常用热点样例。

---

## 下一步建议

### 方案 A：继续稳定当前动作化输出

优先做以下增强：

1. 固定一套动作类型枚举表
2. 给每类动作补充标准参数定义
3. 增加动作间依赖关系，例如：
   - 主帖发布后才能触发评论
   - 评论触发后才能执行二次回复
4. 增加失败回退动作

这条路线最适合继续往“可执行策略引擎”推进。

### 方案 B：让 strategy_generator 也接入轻量 LLM

建议不要让 LLM 替代结构，而是只增强：

- 动作说明
- 阶段目标
- 动作触发条件文本
- 内容风格描述

不建议让 LLM 直接生成整个 JSON 主结构，否则容易失控。

### 方案 C：继续优化节点质量

如果后续发现选人质量仍不稳定，可以继续考虑：

- 软过滤低信息节点
- 引入 BM25 或 embedding 召回
- 对 compact 数据集再增加少量语义增强字段

---

## 本轮结论

本轮改动已经把项目从“输出传播策略说明”推进到了“输出结构化动作计划”。

这是一个重要变化，因为它意味着后续的工作重点可以从“如何描述策略”逐步转向“如何执行策略”。
